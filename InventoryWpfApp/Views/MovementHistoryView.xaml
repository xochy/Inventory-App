<UserControl x:Class="InventoryWpfApp.Views.MovementHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InventoryWpfApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Movement History" FontSize="20" FontWeight="Bold" Margin="0,0,0,15" Grid.Row="0"/>
        <DataGrid ItemsSource="{Binding Movements}" AutoGenerateColumns="True" IsReadOnly="True" Grid.Row="1" Margin="0,0,0,10"/>
        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Left">
            <Button Content="Refresh History" Command="{Binding RefreshMovementsCommand}" Padding="10"/>
            <TextBlock Text="{Binding Message}" Margin="10,0,0,0" VerticalAlignment="Center" Foreground="Blue"/>
        </StackPanel>
    </Grid>
</UserControl>
