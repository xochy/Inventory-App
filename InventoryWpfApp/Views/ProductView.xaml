<UserControl x:Class="InventoryWpfApp.Views.ProductView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InventoryWpfApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Product Management" FontSize="20" FontWeight="Bold" Margin="0,0,0,20" Grid.Row="0" Grid.ColumnSpan="2"/>

        <TextBlock Text="Product Name:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,5,10,5"/>
        <TextBox Text="{Binding NewProductName, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="1" Margin="0,5,0,5" Padding="5"/>

        <TextBlock Text="Description:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,5,10,5"/>
        <TextBox Text="{Binding NewProductDescription, UpdateSourceTrigger=PropertyChanged}" Grid.Row="2" Grid.Column="1" Margin="0,5,0,5" Padding="5"/>

        <TextBlock Text="Applicability Type:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Margin="0,5,10,5"/>
        <ComboBox SelectedValue="{Binding SelectedApplicabilityType}" Grid.Row="3" Grid.Column="1" Margin="0,5,0,5" Padding="5">
            <ComboBoxItem Content="Administrativo"/>
            <ComboBoxItem Content="Sindicalizado"/>
            <ComboBoxItem Content="Todos"/>
        </ComboBox>

        <StackPanel Orientation="Horizontal" Grid.Row="4" Grid.ColumnSpan="2" HorizontalAlignment="Center" Margin="0,15,0,10">
            <Button Content="Add Product" Command="{Binding AddProductCommand}" Margin="5" Padding="10"/>
            <Button Content="Update Product" Command="{Binding UpdateProductCommand}" Margin="5" Padding="10"/>
            <Button Content="Delete Product" Command="{Binding DeleteProductCommand}" Margin="5" Padding="10"/>
            <Button Content="Clear Selection" Command="{Binding ClearSelectionCommand}" Margin="5" Padding="10"/>
        </StackPanel>

        <TextBlock Text="{Binding Message}" Grid.Row="5" Grid.ColumnSpan="2" Margin="0,10,0,0"
                   HorizontalAlignment="Center" Foreground="Blue" TextWrapping="Wrap"/>

        <DataGrid ItemsSource="{Binding Products}" SelectedItem="{Binding SelectedProduct}"
                  AutoGenerateColumns="True" IsReadOnly="False" CanUserAddRows="False"
                  Grid.Row="6" Grid.ColumnSpan="2" Margin="0,20,0,0"/>
    </Grid>
</UserControl>
